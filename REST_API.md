# PepperX REST API

> Stub — the full reference is written in Phase 15. The route surface is defined
> in `PEPPERX_PLAN.md` §11.2, and the live contract is always available at
> `GET /openapi.json` and the Swagger UI once the server is running.

The native REST API is the most complete surface: container and object CRUD,
enumeration/search over labels and tags, existence checks, metadata inspection,
admin/statistics, rehydration, and request history. Base path: `/v1.0`.
