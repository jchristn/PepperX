namespace Test.Performance
{
    using System;
    using System.Threading.Tasks;

    /// <summary>
    /// PepperX performance and throughput harness. Full workload engine is implemented in Phase 11.
    /// </summary>
    public static class Program
    {
        /// <summary>
        /// Main entry point.
        /// </summary>
        /// <param name="args">Command-line arguments.</param>
        /// <returns>Exit code.</returns>
        public static Task<int> Main(string[] args)
        {
            Console.WriteLine("PepperX performance harness is not yet wired (Phase 11).");
            return Task.FromResult(0);
        }
    }
}
