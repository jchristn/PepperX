# Assets

`logo.svg` is a placeholder wordmark used by the README and the dashboard.

The following binary assets are **pending real design work** and must be added
before a public release (this is a human-intervention item — see the handoff):

- `logo.png` — raster logo for Docker Hub and NuGet (referenced by absolute URL)
- `logo.ico` — Windows application/tray icon
- `favicon.ico` — dashboard favicon

Until they exist, `logo.svg` stands in wherever a placeholder is acceptable.
